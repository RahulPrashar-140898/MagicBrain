import React from 'react';

const faceRecognitionComponent = ({imageUrl}) => {
    console.log(imageUrl)
    return (
        <div>
            <h1> Hello</h1>
        </div>
    );
};

export default faceRecognitionComponent;